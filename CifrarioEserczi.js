var charat = ['a', 'b', 'c','d','e']

function cipher(){
    // for(let i = 0; i < charat.length; i++){
           for(num in charat){
               console.log(num)
        }
    }
// }

cipher()// a seconda delle lettere restituisce i numeri, ordine alfabetico coincide con l'ordine cardinale