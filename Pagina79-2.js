function ReverseArray(numbers=[]){
    // for(let i=0; i < numbers.length; i++){
    //     for(let y=0; y < numbers.length; y++){     how to make whithout reverse metod
    //         numbers[i++] = numbers[y--]
    //     }
    // }
    return numbers.reverse()
}

console.log(ReverseArray([1,4,6,9,10]))       